import { BboxType } from "../type";

class	GreyModel {
	private	token: string;

	constructor( token: string ) {
		this.token = token;
	};

	private async	fetch( bbox: BboxType ) {
		
	}
};

export default	GreyModel;
